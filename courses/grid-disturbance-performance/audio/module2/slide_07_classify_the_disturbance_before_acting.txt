# pronunciation: OCP -> O C P; POI -> point of interconnection; PFAPR -> post-fault active power recovery; p.u. -> per unit; PSU -> P S U; UPS -> U P S; VFD -> V F D; OEM -> O E M; SVC -> S V C; E-STATCOM -> E statcom.

The animation compares three constant-depth events. Each row carries its own voltage, duration, and applicable time limit. The marker shows elapsed event time, while the end of the shaded allowance shows the minimum required ride-through interval.

The first example is zero point eight five per unit for one point five seconds. That voltage lies in the two-second band. The example stays within the required interval, and grid active-power consumption continues, with proportional reduction permitted.

The second example is zero point seven zero per unit for zero point four zero seconds. Its band requires half a second, so this event also stays within the minimum ride-through interval. The continued-consumption rule still applies.

The third example is zero point three zero per unit for zero point two zero seconds. Its band requires zero point one five seconds. This event lasts longer than that minimum, so backup transfer is permitted under the table's out-of-envelope treatment. The permission does not mean transfer is compulsory.

Don't compare the visible progress of one row with the allowance on another. The same elapsed time can have different implications at different voltage depths. And don't extend these simple examples into a rule for accumulating multiple faults. The engineering habit to retain is the sequence: classify magnitude, compare time, identify power behavior, and then evaluate the applicable recovery obligation.
