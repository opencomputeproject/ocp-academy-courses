Use the next two questions to check the black-box model. The first asks what the OCP specification actually controls. The second asks you to identify functions that belong inside the five-component decomposition. Remember that a representative conversion diagram explains energy flow, while requirements are established at the product interfaces and behaviors.

For the multi-select question, stay inside the product boundary. Medium-voltage input, low-voltage DC output, conversion, firmware and communications, and auxiliary power are the five named domains. A cooling distribution unit may interact with a data center power system, but it is not one of those five SST component labels. For the first question, remember that diagrams in this course are explanatory. Compliance comes from the measurable interface values and behaviors in the specification.
