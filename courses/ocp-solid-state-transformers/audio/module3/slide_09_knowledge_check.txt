Use the table values to answer both questions. The first checks the two input configurations. The second asks you to distinguish fixed product targets from entries that remain recommended or TBD. Accuracy here depends on keeping the number and its qualifier together.

The first question checks a direct table lookup. The second checks specification literacy. A value can be numerically specific and still be labeled recommended. Another value can define a tolerance while leaving the test stimulus TBD. Select all entries whose status leaves room for clarification or future revision. Do not select the basic unipolar eight-hundred-volt interface; that is a core product definition in this revision.
