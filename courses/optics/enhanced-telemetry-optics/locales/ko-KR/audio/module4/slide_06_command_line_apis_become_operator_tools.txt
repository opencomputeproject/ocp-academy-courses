# pronunciation: CMIS -> C M I S; CDB -> C D B; I2C -> I squared C; SONiC -> sonic; PMD -> P M D; VDM -> V D M; DDMI -> D D M I; SNR -> S N R; PAM4 -> PAM four; MPI -> M P I; FEC -> feck; BER -> B E R; LOS -> L O S; LOL -> L O L; SDK -> S D K; API -> A P I
호스트 SDK와 명령줄 API에서 이 명세는 운영자에게 유용하게 활용됩니다. 명세에는 현장 워크플로우와 직접 연결되는 카테고리가 나열되어 있습니다.

표준 C M I S 접근을 통해 소프트웨어가 로컬 모듈 상태를 읽을 수 있습니다. 원격 C M I S 접근을 통해 소프트웨어가 원격 엔드포인트에서 표준 데이터를 추출할 수 있습니다. 텔레메트리 다운로드는 로컬 또는 원격 모듈 매개변수를 노출합니다. 이벤트 로그 명령어는 최근 이벤트를 읽고, 로그를 삭제하며, 히스테리시스를 조정합니다. 펌웨어 명령어는 로컬 또는 원격 업데이트 워크플로우를 관리합니다.

명세에는 에포크 타임과 모듈 링크 등급 표시도 포함되어 있습니다. 이 범주들은 증거 사슬을 연결하기 때문에 중요합니다: 현재 가치, 과거 사건, 시간 상관관계, 그리고 간결한 상태 점수. 좋은 API는 검증와 인시던트 시 그 체인을 사용할 수 있게 만듭니다.
API 카테고리는 문서화와 교육에도 매핑되어야 합니다. 운영자는 어떤 명령이 현재 텔레메트리를 가져오는지, 어떤 명령어가 최근 이벤트를 가져오는지, 어떤 명령어가 기록을 지우며, 어떤 명령이 동작을 변경하는지 알아야 합니다.
