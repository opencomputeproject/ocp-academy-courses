# pronunciation: M-PNP -> "M P N P"; Fru -> "Fru" (read as the word, not letters); FPGA -> "F P G A"; JSON -> "Jason"

Module four. Interoperability, conformance, tooling, and adoption. This is the bring-it-home module. We'll cover the Interop specification — the glue that holds the bundle together — the JSON schemas and mockups that ship with version zero point five, the open-source tools you'll use to generate your own files, the roadmap from version zero point five to one point zero, and how to join the workstream. Let's wrap this course up.
