# pronunciation: Fru -> "Fru" (read as the word, not letters); FPGA -> "F P G A"; JSON -> "Jason"

By the end of this module you'll know what the Interop specification adds beyond the Fru and FPGA specs, what JSON schemas and mockup examples ship in the version zero point five bundle, what tools exist today and what's coming, where the workstream is in the timeline from version zero point five to version one point zero, and how you personally can contribute. Short module — five content slides plus the course-complete recap. Let's begin with the Interop spec.
