The performance tables in version zero point seven point zero show how H B F builds bandwidth through parallelism.

The defined density calculation uses sixteen Nand dies and produces a total cube size of five hundred twelve gibibytes. The stack exposes sixteen independent host channels. Each channel has a sixty-four-bit data width, which is eight bytes, and the highest speed grade runs at 32 gigatransfers per second.

At that rate, one channel provides 256 gigabytes per second of raw bandwidth. Across sixteen channels, the raw total is four thousand ninety-six gigabytes per second. The table applies a seventy-five percent AXI link-layer efficiency, producing three thousand seventy-two gigabytes per second of effective bandwidth.

The specification defines three grades. Grade one runs at 8 gigatransfers per second and targets zero point three eight four terabytes per second of maximum user bandwidth. Grade two runs at 16 gigatransfers per second and targets one point five three six terabytes per second. Grade three runs at 32 gigatransfers per second and targets three point zero seven two terabytes per second.

Grade one supports a maximum stack height of eight. Grades two and three support sixteen. A channel may also expose one, two, or four virtual AXI interfaces, giving the host options for address interleaving.

These are architecture targets, not a promise that every workload reaches the maximum. Full bandwidth depends on transfer size, address placement, channel utilization, and the Nand access pattern. The rest of the course explains those conditions.
