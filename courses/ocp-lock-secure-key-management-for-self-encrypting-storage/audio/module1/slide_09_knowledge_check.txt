# pronunciation: OCP -> "O C P"; L.O.C.K. -> "lock"; KMB -> "K M B"; MEK -> "meck"
Before you finish, check the trust-boundary mental model.

The first question asks which component is allowed to read a raw Media Encryption Key and program the encryption engine. The second asks you to identify attacker capabilities that the lock threat model explicitly assumes.

Submit both questions once to continue. Correct answers aren't required, and you can retry after reading the feedback. Use that feedback to separate the protected asset and trusted components from the policy responsibilities left to drive firmware.
