# pronunciation: OCP -> "O C P"; L.O.C.K. -> "lock"; TCG -> "T C G"; MEK -> "meck"; MPK -> "M P K"; HPKE -> "H P K E"; KMB -> "K M B"; Opal -> "Opal"; DPE -> "D P E"; UID -> "U I D"; EMEA -> "E M E A"; SP 800-108 -> "S P eight hundred one-oh-eight"; 1.1 -> "one point one"; 2026 -> "twenty twenty-six"
Host protocols expose user intent. The mailbox translates that intent into a smaller protected vocabulary.

Start with H P K E discovery. Firmware asks the K M B for supported algorithms and active handles, retrieves each public key, and uses its M C U D P E context to endorse that key. A host can then validate the certificate chain before sealing an access key. Rotating a certificate-table entry maps to rotating the underlying H P K E keypair.

The M P K lifecycle maps to the T C G Media Encryption Key Multiparty Authorization feature. Initializing an access condition generates a Locked M P K. Changing its access key rewraps it. Enabling the access condition produces an Enabled M P K. Testing the key returns the nonce-bound digest. Binding an encryption-key entry to that access condition becomes a mix M P K operation during media-key establishment.

Ordinary T C G Opal and Enterprise flows may generate, load, or derive media keys with zero M P Ks. Key Per I O uses derive meck when an imported data-encryption key and key tag become the D P K and metadata inputs.

The twenty twenty-six O C P E M E A Summit update explained why standards work and implementation work had to advance together. The group contributed across Caliptra, T C G, and NIST-facing requirements. Implementation feedback also simplified the version one point one key ladder by replacing an earlier bespoke key-combination construct with the standard S P eight hundred one-oh-eight K D F.

That is the integration lesson: stable host semantics, narrow protected commands, and standard cryptographic building blocks reduce the amount of security-sensitive custom behavior each vendor must invent.
